const express = require('express'); // Important de express
const app = express(); // Création d'une application express


module.exports = app; // Exportation de la const app pour y accèder depuis les autres fichiers.